---
name: 订序夜航账本
description: 登录前后统一的年轻深色个人订阅账本设计系统
colors:
  night: "#050608"
  panel: "#0b0e14"
  ink: "#f7f7fb"
  secondary: "#a4a6b2"
  divider: "rgba(255,255,255,.10)"
  violet: "#7048ff"
  pink: "#ff376f"
  cyan: "#13b7d8"
  due-yellow: "#ffd35f"
  active-mint: "#6fe7be"
  danger: "#ff8d8d"
typography:
  family: "Avenir Next, PingFang SC, Noto Sans CJK SC, Microsoft YaHei, sans-serif"
  display: "clamp(28px, 3vw, 42px) / 1.08 / 800"
  title: "20px / 1.25 / 750"
  body: "14px / 1.7 / 400"
  label: "12px / 1.4 / 700"
rounded:
  control: "10px"
  panel: "15px"
  modal: "20px"
  pill: "999px"
---

# Design System: 订序夜航账本

## North star

订序是夜间也能安心查看的个人订阅仪表与账本。登录前负责解释价值，登录后负责处理金额、日期和状态；两者必须共享同一哑黑背景、白色读数、紫红主操作和克制动效，不能登录后切换成另一套浅色后台。

## Hierarchy

- 金额、续费日期、订阅状态优先于装饰和英文标签。
- 主标题使用紧凑负字距和高字重，正文保持 4.5:1 以上对比度。
- 金额与数量使用制表数字，单位和说明降低一级，不与主数值竞争。
- 不依赖颜色单独表达取消、失败或待处理，必须保留状态文字。

## Color rules

- `night` 是登录前后页面底色，`panel` 是工作区和弹窗底色。
- 紫到玫红的短渐变只用于主操作、当前导航和极少数身份标记。
- 黄色表示临近扣款或需要关注；薄荷表示正常生效；红色表示取消或危险操作；青色表示安全和辅助信息。
- 输入框、筛选器和次级按钮使用比面板略亮的暗色表面，不回退到浅色旧组件。

## Layout

- 桌面端：固定侧栏、顶部工具区和最大 1320px 内容区。
- 摘要区保留四项关键数据，但以主卡和普通卡形成层级，不做同权企业指标墙。
- 900px 以下隐藏侧栏并启用底部导航；620px 以下摘要改为单列。
- 卡片圆角 12–16px，弹窗 20px；卡片使用单一边界或环境影，避免双重“幽灵卡片”。

## Motion

- 页面进入只出现一次：18px 上移、轻微模糊消散，最长 720ms。
- 按钮和可操作卡片在 220–280ms 内轻微上移并增强阴影。
- 弹窗通过暗色遮罩保持上下文，不添加与任务无关的漂浮或循环动画。
- `prefers-reduced-motion: reduce` 时关闭动画与过渡。

## Components

### Navigation

- 非当前项使用中性灰；悬停转白；当前项使用紫红渐变。
- 桌面侧栏和移动底栏共用相同激活色与焦点样式。

### Data cards

- 关键读数白色、制表数字；说明使用 `secondary`。
- 主摘要可使用低频紫色径向光晕，普通卡片保持纯暗色。
- 状态色只出现在小面积标记、按钮或边界，不覆盖正文阅读区。

### Forms and modals

- 弹窗、输入、下拉框、日历、分析和账号中心全部使用暗色表面。
- 主提交按钮为紫色或紫红渐变，危险操作为低饱和红色表面。
- 键盘焦点为 3px 紫色外轮廓，不能只改变颜色。

## Do / don't

- **Do** 登录前后复用同一套颜色、字体、圆角、焦点和动效。
- **Do** 让空状态、加载失败和取消状态保持足够对比度。
- **Do** 优先使用原生 CSS 与现有结构，避免为换肤引入新依赖。
- **Don't** 使用浅色奶油后台、衬线金额字体或大量暖绿色旧样式。
- **Don't** 用装饰性玻璃、渐变文字或循环漂浮分散对账注意力。
- **Don't** 改动订阅数据接口、账号隔离和同步行为来配合视觉设计。
