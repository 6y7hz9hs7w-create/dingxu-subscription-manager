# 订序｜个人订阅与续费管理

> 本文根据项目开发对话整理，用于后续维护、发布与功能迭代。文中不记录任何密钥、验证码、访问令牌或账号密码。

## 1. 产品定位

“订序”是一款个人订阅管理微信小程序，用来记录会员、软件和服务订阅，查看续费日历与消费分析，并通过微信订阅消息在用户主动授权后发送续费提醒。

核心目标：让用户清楚知道“订了什么、何时续费、每月花多少、该不该继续订”。

## 2. 当前产品能力

### 订阅管理

- 新增、编辑、暂停、恢复、计划取消和永久删除订阅。
- 支持月付、年付等账期；月付会持续出现在后续月份日历中。
- 支持人民币和美元录入；美元在录入时按汇率换算并显示人民币估值。
- 可按分类提供常用服务候选项。
- 支持服务图标：内置常用服务图标、联网匹配图标、自定义上传图片和自定义颜色。
- 单个微信账号最多保存 100 项订阅。

### 续费与留存闭环

- 到期订阅会进入“待确认”状态。
- 用户可选择：已续费、已在服务商取消、暂停订阅、延后 1/3/7 天处理。
- 确认续费后自动顺延账期；需要时重新申请下一次提醒授权。
- 计划取消的订阅 7 天后自动归档，不参与总览、日历和消费统计；归档记录可恢复或删除。
- 暂停订阅不受自动归档影响。
- 首页有“添加满 3 项订阅”的使用引导。

### 日历与分析

- 续费日历支持月份切换、点击日期查看对应订阅详情，并可直接进入管理操作。
- 总览展示月均支出、年度预计、未来 7 天扣款与即将续费项目。
- 消费分析包含未来续费趋势、分类支出结构、服务名称明细等可视化数据。
- 分析页中的详情改为页面内展示，避免不必要的弹窗。

### 账户、隐私与数据

- 使用微信云开发的 OPENID 识别当前用户，实现数据隔离。
- 微信昵称和头像属于可选资料，仅在用户主动授权后保存并用于个人中心展示。
- 未完善昵称头像不影响订阅管理功能。
- “我的”页面包含账号资料、提醒中心、导出中心、历史记录、清除缓存和问题反馈入口。
- 可导出：全部订阅、本月支出报告、年度订阅账单（图片或 CSV）。
- 提供默认关闭的最小化体验统计开关。

### 提醒中心

- 展示已开启提醒、尚未授权、已发送需重新开启、提醒失败等状态。
- 微信订阅消息采用“一次授权，对应一次提醒”的机制，不是每月自动重复推送。
- 同一用户同一天多项续费会合并成一条提醒，减少打扰。

### 服务处理中心

- 聚合近期续费、30 天内续费、状态跟进任务。
- 提供前往官方渠道续费或取消的操作说明；链接与步骤以服务商实际页面为准。
- 优惠助手及其云端逻辑已经移除，当前不再展示或调用。

## 3. 技术架构

### 小程序

- 项目目录：`wechat-miniprogram/miniprogram`
- 技术：微信原生小程序（WXML、WXSS、JavaScript）。
- 页面：总览、续费日历、添加订阅、消费分析、个人中心等。
- 样式方向：年轻化紫蓝色体系、轻量液态玻璃效果、移动端优先。

### 云开发

- 云环境：腾讯云 CloudBase / 微信云开发。
- 数据库主集合：`subscriptions`。
- 订阅数据必须由云函数按当前 OPENID 过滤，客户端不可指定或篡改数据归属。
- 当前主要云函数：
  - `subscriptionService`：用户资料、订阅增删改查、续费处理、提醒、归档等核心业务。
  - `webSyncService`：曾用于网页同步；网页方向已暂停，不作为当前小程序主流程。
  - `offerService`：优惠助手旧功能相关，优惠助手移除后不再作为产品功能依赖。

### 定时任务

- `subscriptionService` 包含用于日常检查的定时触发逻辑。
- 到期后检查待确认状态、取消订阅归档、提醒发送等均以服务端逻辑为准。

## 4. 云端与权限原则

- 数据库 `subscriptions` 设置为“所有用户不可读写”，只允许云函数读写。
- 用户身份使用云函数通过 `cloud.getWXContext()` 获取的 OPENID，不信任客户端传入的 ownerOpenid。
- 不在小程序代码中保存 AppSecret、访问令牌或其他敏感凭据。
- 如新增需要服务端读写的集合，也应保持“所有用户不可读写”，由云函数代管。

## 5. 微信审核与隐私要点

- 小程序应允许用户先浏览核心功能，再自行选择是否补充头像、昵称等资料；不能在打开首页时强制请求头像、昵称或手机号。
- 当前昵称、头像均为可选资料，符合“先体验、后授权”的审核调整方向。
- 隐私保护指引应声明：
  - 微信用户标识（OPENID）：识别当前微信用户并隔离数据。
  - 订阅管理信息：保存名称、金额、账期、续费日期、分类、备注等，用于日历、分析与提醒。
  - 微信昵称/头像：用户主动授权后用于个人中心展示。
  - 选中的照片或视频：仅用于用户自定义订阅服务图标。
  - 剪切板：仅在用户主动导出 CSV 且设备无法直接分享时使用。
- 审核版本的测试账号如非必要不要填写真实账号或密码。

## 6. 版本演进摘要

### 早期版本（1.x）

- 完成基础订阅录入、续费日历、消费分析与微信云开发数据存储。
- 处理了日历、分析页无法打开、移动端网页不可访问、标记色块显示异常等基础问题。
- 接入微信无感身份识别与个人中心。

### 2.0 系列

- 重做整体视觉、导航与数据展示。
- 增加自定义图标、分类常用服务、颜色选择、订阅编辑与取消流程。
- 加入提醒中心、导出功能、分析图表、服务处理入口。

### 2.2 至 2.5 系列

- 完成取消、暂停、恢复、归档与自动清理策略的多轮优化。
- 优化卡片状态颜色、服务名称与分类展示、日历交互、美元换算、图标匹配和性能缓存。
- 提醒改为用户每次主动授权后仅发送一次。

### 留存版

- 建立“到期 → 待确认 → 已续费/取消/暂停/延后”的完整处理闭环。
- 同日提醒合并、取消 7 天后自动归档、历史记录独立入口、添加订阅引导和 100 项上限均已落地。

### 服务版

- 加入服务处理中心与官方渠道操作指引。
- 后续曾尝试优惠助手，现已按产品决策完整删除。

## 7. 当前代码与日志位置

| 内容 | 位置 |
| --- | --- |
| 小程序源代码 | `wechat-miniprogram/miniprogram` |
| 核心云函数 | `wechat-miniprogram/cloudfunctions/subscriptionService` |
| 自动化测试 | `tests/wechat-miniprogram.test.mjs` |
| 项目主说明 | `README.md` |
| 小程序说明 | `wechat-miniprogram/README.md` |
| 本总结 | `订序小程序项目总结.md` |

运行测试：

```bash
npm test
```

## 8. 最近完成的修复

### 个人中心续费处理卡片横向溢出

问题：删除优惠助手后，在“我的”页面出现横向溢出；续费处理卡片的价格、服务信息和操作区域把页面撑宽。

修复：

- 为个人中心和续费处理卡片补充安全宽度与横向溢出限制。
- 将价格从服务行的第三列移动至服务名称下方，避免服务名与金额在窄屏竞争宽度。
- 确保按钮网格使用可收缩列，避免文本或按钮撑开容器。

相关文件：

- `wechat-miniprogram/miniprogram/pages/settings/settings.wxml`
- `wechat-miniprogram/miniprogram/pages/settings/settings.wxss`

验证结果：自动化测试当前 34 项全部通过。

## 9. 发布流程（小程序）

1. 在微信开发者工具打开 `wechat-miniprogram` 项目。
2. 点击“编译”，检查总览、日历、添加、分析、我的五个页面。
3. 如云函数有改动：右键相应云函数，选择“创建并部署：云端安装依赖”。
4. 在真机预览中检查：登录/可选资料、添加订阅、提醒授权、续费处理、导出、归档记录。
5. 点击“上传”，填写中文项目备注。
6. 到微信公众平台的版本管理中提交审核；提交前同步检查隐私保护指引与测试流程。

## 10. 后续维护建议

- 每次新增数据字段时，同时检查：云函数校验、导出、日历、分析、提醒、历史归档是否需要适配。
- 每次调整卡片布局时优先在窄屏真机或开发者工具小尺寸模拟器验证，尤其检查金额、服务名、长按钮和三列操作区。
- 功能保持克制：订阅管理的核心是记录、提醒、确认和复盘；不引入需要长期维护的自动抓取或非必要外部服务，除非有明确且稳定的数据来源。
- 发布前至少运行一次 `npm test`，并检查开发者工具的编译错误与警告。

## 11. 2026-08-13 续费处理界面与 GitHub 归档记录

### 续费处理界面

- 调整“我的”页的续费处理卡片，统一服务信息、金额、主操作、次操作与延后操作的阅读顺序和左右基线。
- 主操作“已续费，顺延账期”、暂停订阅和服务商取消统一为同高度、同圆角、同字号的决策按钮；颜色只表达操作性质，不再通过尺寸制造额外层级。
- 次操作与延后处理按钮采用独立间隔，避免黄色、红色等状态色块相连或贴近卡片边缘。
- “金额有变化”与价格同一行展示，改为带下划线的小型文字操作；点击后在相邻位置展开金额输入框，降低视觉干扰并保持信息关联。
- 移除处理中右上角“退出”按钮，用户需对当前订阅选择续费、暂停、取消或延后处理；流程完成页保留“完成”入口。
- 调整时遵循移动端触控与布局规范：可点击区域保持足够高度、相邻操作保持明确间距、同组组件共享固定的左右内容边界。

相关文件：

- `wechat-miniprogram/miniprogram/pages/settings/settings.wxml`
- `wechat-miniprogram/miniprogram/pages/settings/settings.wxss`

### 项目归档与远程维护

- 已生成本地源码归档包：`dingxu-subscription-manager-2026-08-13.zip`；压缩包排除了 `.git`、`node_modules` 和系统杂项，源目录保持不动。
- 已创建并推送至 GitHub 仓库：`https://github.com/6y7hz9hs7w-create/dingxu-subscription-manager`。
- 本地 `main` 已追踪 `origin/main`；后续修改后执行 `git add -A`、`git commit -m "说明本次修改"`、`git push` 即可同步。
- 为统一项目归属，已将现有提交历史中的作者和提交者重写为 `leoblaze7 <6y7hz9hs7w-create@users.noreply.github.com>`，并强制同步至远程。GitHub Contributors 页面在历史重写后可能需要约 24 小时刷新。
- 本节不记录 GitHub Personal Access Token、密码或其他凭据；后续如需重新授权，应创建仅限该仓库、仅授予 `Contents: Read and write` 的短期 Token。
